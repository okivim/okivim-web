import Link from "@docusaurus/Link";
import MastheadSlider from "./MastheadSlider.mdx";
import FloatingLines from "./FloatingLines.mdx";
import styles from "./styles.module.css";

export default function HomepageMasthead({
  title,
  subtitle,
  ctaLabel,
  ctaHref,
}) {
  return (
    <section className={`masthead ${styles.masthead}`}>
      <div className={styles.background}>
        <FloatingLines />
      </div>
      <div className={styles.inner}>
        <div className={styles.content}>
          <h1 className={styles.title}>{title}</h1>
          <p className={styles.subtitle}>{subtitle}</p>
          <div className={styles.actions}>
            <Link
              className={`button button--secondary button--lg ${styles.cta}`}
              to={ctaHref}
            >
              {ctaLabel}
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                strokeWidth="2"
                strokeLinecap="round"
                strokeLinejoin="round"
                className={styles.ctaIcon}
              >
                <path d="M5 12h14" />
                <path d="m12 5 7 7-7 7" />
              </svg>
            </Link>
          </div>
        </div>
        <MastheadSlider />
      </div>
    </section>
  );
}
